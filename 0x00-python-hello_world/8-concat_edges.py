#!/usr/bin/python3
str = "Python is an interpreted language, meaning that it's processed and executed by an interpreter, rather than being compiled to machine code"
str = str[39:67] + str[107:112] + str[:6]
print(str)
